generator cliet {
    provider = 'prisma-client-js'
  }
datasource db {
    privider = 'mysql'
    url      = env('DATABASE_URL')
  }
model user {
    id          Int     @id @default(autoincrement())
    firstName   String
    lastName    String
    email       String  @unique
    occupation  String?
  }
